[project]
name = "file-splitter"
version = "0.1.0"
description = "A simple script to split a text file into several chunks."
requires-python = ">=3.11,<3.12"
dependencies = []

[dependency-groups]
dev = [
    "pylint",
    "autopep8",
]
